# Прогресс разработки Арканоида

## 📋 Общая информация
Проект: Арканоид (Arkanoid/Breakout)
Базируется на структуре игры Змейка
Используется SFML для графики

---

## ✅ Выполненные задачи

### 1. Рефакторинг с наследованием
- [x] Создан базовый класс `GameObject` для всех игровых объектов
- [x] Класс `Ball` наследуется от `GameObject`
- [x] Класс `Paddle` наследуется от `GameObject`
- [x] Все изменения объединены в ветку `master`

### 2. Класс Ball (Шарик)
- [x] Созданы файлы `Ball.h` и `Ball.cpp`
- [x] Наследование от `GameObject`
- [x] Реализован конструктор
- [x] Реализован метод `initializeShape()`
- [x] Реализован метод `update()` (базовая версия с движением)
- [x] Реализован метод `draw()`
- [x] Реализован метод `getBounds()`
- [x] Геттеры и сеттеры
- [x] Начальная позиция: на платформе по центру
- [x] Начальная скорость: 300
- [x] Начальное направление: 60° от горизонтали
- [x] Радиус: `CELL_SIZE`

### 3. Класс Paddle (Платформа)
- [x] Созданы файлы `Paddle.h` и `Paddle.cpp`
- [x] Наследование от `GameObject`
- [x] Размеры: ширина = 3 клетки, высота = 1 клетка
- [x] Размеры хранятся внутри класса (не в Constants.h)
- [x] Реализован конструктор
- [x] Реализован метод `initializeShape()`
- [x] Реализован метод `update()` с управлением:
  - Клавиатура (стрелки ← →) - приоритет
  - Мышь (опционально, если клавиатура не используется)
- [x] Реализован метод `draw()`
- [x] Реализован метод `getBounds()`
- [x] Скорость движения: 200
- [x] Геттер `getSize()`

### 4. Класс Block (Блок)
- [x] Созданы файлы `Block.h` и `Block.cpp`
- [x] Наследование от `GameObject`
- [x] Добавлены в структуру проекта (.vcxproj и .vcxproj.filters)
- [ ] Реализация конструктора
- [ ] Реализация методов

---

## 🔄 Текущие обсуждения и решения

### Структура блоков и коллекция
**Решение:** Блоки будут разных типов (наследники базового `Block`)
- `Block` - базовый класс, наследуется от `GameObject`
- Наследники: `SimpleBlock`, `StrongBlock`, `BonusBlock`, `UnbreakableBlock`
- Коллекция: `std::vector<std::unique_ptr<Block>>` в классе `Game`
- Причина: полиморфизм для разных типов блоков
- Использование умных указателей (`std::unique_ptr`) для автоматического управления памятью

**Статус:** Решено, но не реализовано

### Границы экрана и коллизии
**Решение:** Простая проверка границ без создания отдельного объекта
- Левая граница: `position.x < 0`
- Правая граница: `position.x > SCREEN_WIDTH`
- Верхняя граница: `position.y < 0`
- Нижняя граница: проигрыш (шарик упал)
- Проверка выполняется напрямую в логике обработки движения шарика
- Унификация через отдельный объект не требуется (избыточно для простых границ)

**Статус:** Решено, будет реализовано вместе с системой коллизий

### Позиционирование блоков
**Вопрос:** Как будут позиционироваться блоки?
- По сетке (как в змейке) или свободное позиционирование?
- **Статус:** Не определено

---

## 📝 Заметки

### Теоретические моменты
- Наследование транзитивно: наследник наследника имеет доступ к `public` и `protected` членам всех предков
- Умные указатели (`std::unique_ptr`) - правильный выбор для полиморфных коллекций
- `std::vector` достаточен для блоков (добавление только в конец)
- Для змейки использовали `std::deque` (добавление в начало при росте)

### Архитектурные решения
- Столкновения проверяются глобально (не внутри классов объектов)
- Коллизии реализованы централизованно в основном игровом цикле

### Элегантная логика столкновений (рабочая реализация)
**Подход:** Определение стороны столкновения через диапазоны координаты X шарика
- **Преимущества:**
  - Простая и понятная логика (не требует вычисления перекрытий по Y)
  - Учитывает движение шарика через `Δx = deltaTime * velocity.x`
  - Меньше вычислений, более явная логика
- **Реализация:**
  - Случай 2а (боковое слева): `x1 ≤ X ≤ x1 + Δx + ε` → `reflectX()`
  - Случай 2б (боковое справа): `x2 + D - Δx - ε ≤ X ≤ x2 + D` → `reflectX()`
  - Случай 1 (вертикальное): `x1 + Δx + ε ≤ X ≤ x2 - Δx - ε + D` → `reflectY()`
  - Случай 3 (остальное): все остальные ситуации → `reflectY()`
- **Обозначения:**
  - X - правая граница шарика
  - D - диаметр шарика
  - x1, x2 - левая и правая границы блока
  - Δx - перемещение шарика по X за кадр
  - ε = 0.1px - погрешность для безопасного сравнения float
- **Статус:** Рабочая реализация, протестирована, работает корректно

### Будущие возможности
- Прилипание шарика к платформе при старте (пока не требуется, но можно легко добавить)
- Разные типы блоков с разной прочностью и бонусами
- Настройки сложности (влияют на количество блоков)

## 🐛 Известные проблемы (требуют исправления)

### 1. Двойное попадание в блоки
**Проблема:** Шарик одним ударом может уничтожить два блока одновременно
**Причина:** Используется прямоугольник (`getBounds()`) для шарика вместо круга, что создает лишнюю область пересечения
**Решение:** Нужно реализовать точную проверку столкновения круга с прямоугольником или изменить логику коллизий
**Статус:** Запланировано к исправлению

### 2. Заклинивание шарика (чисто вертикальное или горизонтальное движение)
**Проблема:** Шарик может начать двигаться строго вертикально (только вверх-вниз, без горизонтального движения) или строго горизонтально (только влево-вправо, без вертикального движения)
**Причина:** Неизвестна, требует разбора и анализа логики столкновений. Возможно связано с неправильными отражениями при столкновениях
**Решение:** Необходимо проанализировать логику отражений и добавить защиту от чисто вертикального/горизонтального движения (минимальный порог скорости по каждой оси)
**Статус:** Запланировано к исправлению

### 3. Заклинивание шарика в платформе (движущаяся платформа)
**Проблема:** Шарик застревает внутри платформы и "катается" туда-сюда, особенно когда платформа прижата к стенке
**Причина:** Платформа движется, в отличие от блоков. Когда платформа движется под падающим шариком, шарик проваливается внутрь, логика диапазонов X не работает для внутренней позиции
**Попытка решения:** Переход в систему отсчета платформы при столкновении:
  - Вычитаем скорость платформы из скорости шарика перед отражением
  - Проводим отражение (как будто платформа неподвижна)
  - Прибавляем скорость платформы обратно после отражения
**Реализовано:** Добавлено отслеживание `currentVelocityX` в Paddle, методы `getVelocityX()` и `setVelocityX()`, корректировка скорости в `checkBallPaddleCollision()`
**Статус:** Тестируется. Если не сработает - откатить и искать другие варианты (например, проверка направления движения шарика перед столкновением)

---

## 🎯 Следующие шаги

1. Реализовать класс `Block`:
   - Конструктор с параметром позиции (для создания блоков в разных местах)
   - Виртуальные методы для обработки попаданий (`onHit()`, `isDestroyed()`, `getHitsRemaining()`)
   - Защищенные члены для доступа наследников

2. Создать первый тип блока (`SimpleBlock`) как пример реализации

3. Добавить коллекцию блоков в класс `Game`:
   - `std::vector<std::unique_ptr<Block>> blocks;`

4. Реализовать систему коллизий:
   - Столкновения шарика с границами экрана
   - Столкновения шарика с платформой
   - Столкновения шарика с блоками

5. Интеграция в основной игровой цикл

---

## 📚 Ссылки и ресурсы
- [План рефакторинга с наследованием](./INHERITANCE_REFACTORING_PLAN.md)

---

## 🔄 Код для возврата при наступлении часа ИКС

### ⚠️ ВАЖНО: Этот код временно отключен, но должен быть восстановлен когда потребуется функционал ввода имени при рекорде

### 1. Переход в состояние NAME_INPUT при Game Over (в GameMain.cpp, раздел "Переходы между состояниями")

```cpp
if (previousState == GameState::GAME && gameStateManager.getCurrentState() == GameState::GAME_OVER)
{
    soundManager.playSessionEnd();
    if (highScoreManager.isHighScore(game.getScore(), Y))
        gameStateManager.setState(GameState::NAME_INPUT);
}
```

**Где вставить:** После строки `soundManager.playSessionEnd();` внутри блока проверки Game Over

---

### 2. Обработка событий клавиатуры для состояния NAME_INPUT (в GameMain.cpp, раздел обработки событий)

```cpp
else if (gameStateManager.getCurrentState() == GameState::NAME_INPUT)
{
    if (!ui.getIsEnteringName())
    {
        if (event.key.code == sf::Keyboard::W || event.key.code == sf::Keyboard::Up)
            ui.moveUp(menuState);
        else if (event.key.code == sf::Keyboard::S || event.key.code == sf::Keyboard::Down)
            ui.moveDown(menuState);
        else if (event.key.code == sf::Keyboard::Enter)
        {
            int selectedOption = menuState.getSelectedIndex();
            ui.selectNameInputOption(menuState);
            if (selectedOption == 0 && gameStateManager.getCurrentState() == GameState::GAME_OVER)
            {
                highScoreManager.addScore("XYZ", game.getScore());
                highScoreManager.saveHighScores();
                ui.resetNameInput();
            }
        }
    }
    else
    {
        if (event.key.code >= sf::Keyboard::A && event.key.code <= sf::Keyboard::Z)
        {
            char c = 'A' + (event.key.code - sf::Keyboard::A);
            ui.addCharToName(c);
        }
        else if (event.key.code >= sf::Keyboard::Num0 && event.key.code <= sf::Keyboard::Num9)
        {
            char c = '0' + (event.key.code - sf::Keyboard::Num0);
            ui.addCharToName(c);
        }
        else if (event.key.code == sf::Keyboard::Backspace)
            ui.removeLastCharFromName();
        else if (event.key.code == sf::Keyboard::Enter)
        {
            highScoreManager.addScore(ui.getCurrentPlayerName(), game.getScore());
            highScoreManager.saveHighScores();
            gameStateManager.setState(GameState::GAME_OVER);
            menuState.setSelectedIndex(0);
            ui.resetNameInput();
        }
    }
}
```

**Где вставить:** После обработки состояния `GameState::HIGH_SCORES`, перед закрывающей скобкой обработки событий `KeyPressed`

---

### 3. Отрисовка экрана NAME_INPUT (в GameMain.cpp, раздел отрисовки)

```cpp
else if (gameStateManager.getCurrentState() == GameState::NAME_INPUT)
{
    game.draw(window);
    ui.drawNameInputPopup(window, menuState);
}
```

**Где вставить:** После отрисовки состояния `GameState::VICTORY`, перед отрисовкой `GameState::HIGH_SCORES`

---

### 📝 Примечания:
- Все эти участки кода временно закомментированы многострочными комментариями `/* */`
- В коде оставлены комментарии с указанием на этот файл
- Функционал ввода имени полностью сохранен в UI классе и готов к использованию
- При восстановлении просто раскомментировать соответствующие блоки и удалить комментарии-указатели
