# План рефакторинга: Наследование и базовый класс GameObject

## 📊 Анализ текущего состояния

### Общие элементы между Paddle и Ball:
✅ `position` (sf::Vector2f) - позиция объекта  
✅ `update(float deltaTime, ...)` - обновление состояния  
✅ `draw(sf::RenderWindow& window)` - отрисовка  
✅ `getPosition() const` - получение позиции  
✅ `getBounds() const` - получение границ для коллизий  

### Различия:
❌ **Сигнатуры update()**: 
   - `Paddle::update(float deltaTime, const sf::RenderWindow& window)` - требует window
   - `Ball::update(float deltaTime)` - не требует window

❌ **Типы shape**:
   - `Paddle` использует `sf::RectangleShape`
   - `Ball` использует `sf::CircleShape`

❌ **Логика**:
   - `Paddle` - обработка ввода (мышь/клавиатура)
   - `Ball` - физика движения (velocity)

---

## 🏗️ Предлагаемая архитектура

### Базовый класс GameObject

**Файл: `GameObject.h`** (уже создан)

**Принципы:**
- `position` вынесен в базовый класс (общий для всех)
- `update()` принимает опциональный указатель на window (для Paddle)
- `draw()` и `getBounds()` - чисто виртуальные (каждый класс реализует сам)
- Виртуальный деструктор для корректного удаления через указатель на базовый класс

**Ключевые моменты:**
```cpp
virtual void update(float deltaTime, const sf::RenderWindow* window = nullptr) = 0;
```
Опциональный `window` позволяет Paddle получать позицию мыши, а Ball просто игнорирует этот параметр.

---

## 🔄 План рефакторинга классов

### 1. Класс Paddle

**Изменения:**
- Наследуется от `GameObject`
- Удаляется поле `position` (используется из базового класса)
- Сигнатура `update()` меняется на `update(float deltaTime, const sf::RenderWindow* window = nullptr)`
- `getPosition()` удаляется (используется из базового класса)
- `getBounds()` становится override виртуального метода

**Структура после рефакторинга:**
```cpp
class Paddle : public GameObject
{
public:
    Paddle();
    void update(float deltaTime, const sf::RenderWindow* window = nullptr) override;
    void draw(sf::RenderWindow& window) override;
    sf::Vector2f getSize() const;
    sf::FloatRect getBounds() const override;

private:
    sf::RectangleShape shape;
    float width;
    float height;
    float speed;
    void initializeShape();
};
```

**В конструкторе:**
```cpp
Paddle::Paddle()
    : width(3 * CELL_SIZE),
      height(1 * CELL_SIZE),
      speed(200.0f)
{
    position = sf::Vector2f((SCREEN_WIDTH - 3 * CELL_SIZE) / 2.0f, 
                            SCREEN_HEIGHT - 1 * CELL_SIZE - 10.0f);
    initializeShape();
}
```

**В update():**
```cpp
void Paddle::update(float deltaTime, const sf::RenderWindow* window)
{
    if (window) {
        // Логика с мышью
        sf::Vector2i mousePosInt = sf::Mouse::getPosition(*window);
        // ...
    }
    // Логика с клавиатурой
    // ...
}
```

---

### 2. Класс Ball

**Изменения:**
- Наследуется от `GameObject`
- Удаляется поле `position` (используется из базового класса)
- Сигнатура `update()` меняется на `update(float deltaTime, const sf::RenderWindow* window = nullptr)`
- `getPosition()` удаляется (используется из базового класса, inline метод)
- `getBounds()` становится override виртуального метода
- Добавляются методы для отскоков: `bounceHorizontal()`, `bounceVertical()`

**Структура после рефакторинга:**
```cpp
class Ball : public GameObject
{
public:
    Ball();
    void update(float deltaTime, const sf::RenderWindow* window = nullptr) override;
    void draw(sf::RenderWindow& window) override;
    sf::FloatRect getBounds() const override;
    
    // Методы для отскоков
    void bounceHorizontal();
    void bounceVertical();

private:
    sf::CircleShape shape;
    float radius;
    float speed;
    sf::Vector2f velocity;
    void initializeShape();
};
```

**В update():**
```cpp
void Ball::update(float deltaTime, const sf::RenderWindow* window)
{
    // window игнорируется, но сигнатура соответствует базовому классу
    position.x += velocity.x * deltaTime;
    position.y += velocity.y * deltaTime;
    shape.setPosition(position);
}
```

**Методы отскоков:**
```cpp
void Ball::bounceHorizontal()
{
    velocity.x = -velocity.x;
}

void Ball::bounceVertical()
{
    velocity.y = -velocity.y;
}
```

---

### 3. Будущий класс Block/Brick

**Структура (планируется):**
```cpp
class Block : public GameObject
{
public:
    Block(float x, float y, sf::Color color);
    void update(float deltaTime, const sf::RenderWindow* window = nullptr) override;
    void draw(sf::RenderWindow& window) override;
    sf::FloatRect getBounds() const override;
    
    bool isDestroyed() const { return destroyed; }
    void destroy() { destroyed = true; }
    int getPoints() const { return points; }

private:
    sf::RectangleShape shape;
    float width;
    float height;
    bool destroyed;
    int points;
    void initializeShape(sf::Color color);
};
```

**Особенности:**
- `update()` может быть пустым (блоки не двигаются)
- Или может реализовывать анимацию разрушения
- Метод `destroy()` для пометки блока как уничтоженного

---

## 📦 Использование вектора объектов

### Вариант 1: Вектор умных указателей (рекомендуется)
```cpp
#include <vector>
#include <memory>

std::vector<std::unique_ptr<GameObject>> gameObjects;

// Добавление объектов
gameObjects.push_back(std::make_unique<Paddle>());
gameObjects.push_back(std::make_unique<Ball>());
for (int i = 0; i < 10; ++i) {
    gameObjects.push_back(std::make_unique<Block>(x, y, color));
}

// Обновление всех объектов
for (auto& obj : gameObjects) {
    obj->update(deltaTime, &window);
}

// Отрисовка всех объектов
for (auto& obj : gameObjects) {
    obj->draw(window);
}
```

### Вариант 2: Вектор обычных указателей (если объекты хранятся отдельно)
```cpp
std::vector<GameObject*> drawableObjects;

Paddle paddle;
Ball ball;
std::vector<Block> blocks;

drawableObjects.push_back(&paddle);
drawableObjects.push_back(&ball);
for (auto& block : blocks) {
    drawableObjects.push_back(&block);
}

for (auto* obj : drawableObjects) {
    obj->update(deltaTime, &window);
    obj->draw(window);
}
```

---

## ✅ Преимущества предложенной архитектуры

1. **Единый интерфейс** - все объекты имеют одинаковые методы
2. **Полиморфизм** - можно работать с объектами через указатель на базовый класс
3. **Расширяемость** - легко добавлять новые типы объектов (Block, PowerUp, и т.д.)
4. **Управление через вектор** - удобно обновлять и отрисовывать все объекты в цикле
5. **Коллизии** - общий `getBounds()` для всех объектов упрощает проверку столкновений

---

## 🔧 Последовательность внедрения

### Этап 1: Создание базового класса
- ✅ Создать `GameObject.h`
- ⏳ Добавить `GameObject.h` в проект (.vcxproj)

### Этап 2: Рефакторинг Paddle
- ⏳ Изменить наследование: `class Paddle : public GameObject`
- ⏳ Удалить поле `position`, использовать базовое
- ⏳ Изменить сигнатуру `update()` на виртуальную
- ⏳ Реализовать `draw()` (если еще не реализован)
- ⏳ Удалить `getPosition()` (использовать базовый)
- ⏳ Пометить `getBounds()` как `override`

### Этап 3: Рефакторинг Ball
- ⏳ Изменить наследование: `class Ball : public GameObject`
- ⏳ Удалить поле `position`, использовать базовое
- ⏳ Изменить сигнатуру `update()` на виртуальную
- ⏳ Реализовать `draw()` (если еще не реализован)
- ⏳ Удалить `getPosition()` (использовать базовый)
- ⏳ Пометить `getBounds()` как `override`
- ⏳ Добавить методы `bounceHorizontal()`, `bounceVertical()`

### Этап 4: Создание класса Block (будущее задание)
- ⏳ Создать `Block.h` и `Block.cpp`
- ⏳ Наследоваться от `GameObject`
- ⏳ Реализовать все виртуальные методы

### Этап 5: Интеграция в игровой цикл
- ⏳ Создать вектор объектов (или использовать существующие переменные)
- ⏳ Обновить логику обновления и отрисовки для использования полиморфизма

---

## ⚠️ Важные замечания

1. **Виртуальный деструктор** - обязателен в базовом классе для корректного удаления объектов через указатель на базовый класс

2. **Опциональный window** - используется только Paddle, остальные классы просто игнорируют этот параметр

3. **Порядок инициализации** - в конструкторах производных классов `position` инициализируется через присваивание, так как это поле базового класса (нельзя в списке инициализации)

4. **Метод draw()** - сейчас только объявлен, нужно будет реализовать для всех классов

5. **Отскоки** - методы отскоков специфичны для Ball, но могут быть полезны для других объектов в будущем

